// @flow

/* Constants */
import allColors from './Colors';
import allFonts from './Fonts';

const DefaultTheme = {
  dark: false,
  roundness: 4,
  colors: allColors,
  fonts: allFonts,
  animation: {
    scale: 1.0,
  },
};

export default DefaultTheme;
